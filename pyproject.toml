[project]
name = "podcast-bot"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "asyncpg>=0.29",
    "google-api-python-client>=2.100.0",
    "httpx>=0.27",
    "psycopg[binary]>=3.3.3",
    "pydantic-ai>=0.0.49",
    "python-dotenv>=1.0",
    "python-ulid>=2.0",
]

[project.optional-dependencies]
bot = [
    "apscheduler==4.0.0a6",
    "faster-whisper>=1.0",
    "feedparser>=6.0",
    "groq>=0.8",
    "python-telegram-bot>=20.0",
]
web = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
]

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
    "pytest",
    "pytest-asyncio",
    "pytest-mock-resources[docker]>=2.0",
    "pytest-xdist>=3.8.0",
    "ruff>=0.15.6",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
filterwarnings = ["ignore::telegram.warnings.PTBUserWarning"]

[tool.ruff]
line-length = 200

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.ruff.format]
quote-style = "double"
